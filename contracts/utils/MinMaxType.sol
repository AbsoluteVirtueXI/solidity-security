// SPDX-License-Identifier: MIT
pragma solidity ^0.7.0;

contract MinMaxType {
    function minMaxuint8() public pure returns (uint8, uint8) {
        return (type(uint8).min, type(uint8).max);
    }

    function minMaxuint16() public pure returns (uint16, uint16) {
        return (type(uint16).min, type(uint16).max);
    }

    function minMaxuint24() public pure returns (uint24, uint24) {
        return (type(uint24).min, type(uint24).max);
    }

    function minMaxuint32() public pure returns (uint32, uint32) {
        return (type(uint32).min, type(uint32).max);
    }

    function minMaxuint40() public pure returns (uint40, uint40) {
        return (type(uint40).min, type(uint40).max);
    }

    function minMaxuint48() public pure returns (uint48, uint48) {
        return (type(uint48).min, type(uint48).max);
    }

    function minMaxuint56() public pure returns (uint56, uint56) {
        return (type(uint56).min, type(uint56).max);
    }

    function minMaxuint64() public pure returns (uint64, uint64) {
        return (type(uint64).min, type(uint64).max);
    }

    function minMaxuint72() public pure returns (uint72, uint72) {
        return (type(uint72).min, type(uint72).max);
    }

    function minMaxuint80() public pure returns (uint80, uint80) {
        return (type(uint80).min, type(uint80).max);
    }

    function minMaxuint88() public pure returns (uint88, uint88) {
        return (type(uint88).min, type(uint88).max);
    }

    function minMaxuint96() public pure returns (uint96, uint96) {
        return (type(uint96).min, type(uint96).max);
    }

    function minMaxuint104() public pure returns (uint104, uint104) {
        return (type(uint104).min, type(uint104).max);
    }

    function minMaxuint112() public pure returns (uint112, uint112) {
        return (type(uint112).min, type(uint112).max);
    }

    function minMaxuint120() public pure returns (uint120, uint120) {
        return (type(uint120).min, type(uint120).max);
    }

    function minMaxuint128() public pure returns (uint128, uint128) {
        return (type(uint128).min, type(uint128).max);
    }

    function minMaxuint136() public pure returns (uint136, uint136) {
        return (type(uint136).min, type(uint136).max);
    }

    function minMaxuint144() public pure returns (uint144, uint144) {
        return (type(uint144).min, type(uint144).max);
    }

    function minMaxuint152() public pure returns (uint152, uint152) {
        return (type(uint152).min, type(uint152).max);
    }

    function minMaxuint160() public pure returns (uint160, uint160) {
        return (type(uint160).min, type(uint160).max);
    }

    function minMaxuint168() public pure returns (uint168, uint168) {
        return (type(uint168).min, type(uint168).max);
    }

    function minMaxuint176() public pure returns (uint176, uint176) {
        return (type(uint176).min, type(uint176).max);
    }

    function minMaxuint184() public pure returns (uint184, uint184) {
        return (type(uint184).min, type(uint184).max);
    }

    function minMaxuint192() public pure returns (uint192, uint192) {
        return (type(uint192).min, type(uint192).max);
    }

    function minMaxuint200() public pure returns (uint200, uint200) {
        return (type(uint200).min, type(uint200).max);
    }

    function minMaxuint208() public pure returns (uint208, uint208) {
        return (type(uint208).min, type(uint208).max);
    }

    function minMaxuint216() public pure returns (uint216, uint216) {
        return (type(uint216).min, type(uint216).max);
    }

    function minMaxuint224() public pure returns (uint224, uint224) {
        return (type(uint224).min, type(uint224).max);
    }

    function minMaxuint232() public pure returns (uint232, uint232) {
        return (type(uint232).min, type(uint232).max);
    }

    function minMaxuint240() public pure returns (uint240, uint240) {
        return (type(uint240).min, type(uint240).max);
    }

    function minMaxuint248() public pure returns (uint248, uint248) {
        return (type(uint248).min, type(uint248).max);
    }

    function minMaxuint256() public pure returns (uint256, uint256) {
        return (type(uint256).min, type(uint256).max);
    }

    function minMaxint8() public pure returns (int8, int8) {
        return (type(int8).min, type(int8).max);
    }

    function minMaxint16() public pure returns (int16, int16) {
        return (type(int16).min, type(int16).max);
    }

    function minMaxint24() public pure returns (int24, int24) {
        return (type(int24).min, type(int24).max);
    }

    function minMaxint32() public pure returns (int32, int32) {
        return (type(int32).min, type(int32).max);
    }

    function minMaxint40() public pure returns (int40, int40) {
        return (type(int40).min, type(int40).max);
    }

    function minMaxint48() public pure returns (int48, int48) {
        return (type(int48).min, type(int48).max);
    }

    function minMaxint56() public pure returns (int56, int56) {
        return (type(int56).min, type(int56).max);
    }

    function minMaxint64() public pure returns (int64, int64) {
        return (type(int64).min, type(int64).max);
    }

    function minMaxint72() public pure returns (int72, int72) {
        return (type(int72).min, type(int72).max);
    }

    function minMaxint80() public pure returns (int80, int80) {
        return (type(int80).min, type(int80).max);
    }

    function minMaxint88() public pure returns (int88, int88) {
        return (type(int88).min, type(int88).max);
    }

    function minMaxint96() public pure returns (int96, int96) {
        return (type(int96).min, type(int96).max);
    }

    function minMaxint104() public pure returns (int104, int104) {
        return (type(int104).min, type(int104).max);
    }

    function minMaxint112() public pure returns (int112, int112) {
        return (type(int112).min, type(int112).max);
    }

    function minMaxint120() public pure returns (int120, int120) {
        return (type(int120).min, type(int120).max);
    }

    function minMaxint128() public pure returns (int128, int128) {
        return (type(int128).min, type(int128).max);
    }

    function minMaxint136() public pure returns (int136, int136) {
        return (type(int136).min, type(int136).max);
    }

    function minMaxint144() public pure returns (int144, int144) {
        return (type(int144).min, type(int144).max);
    }

    function minMaxint152() public pure returns (int152, int152) {
        return (type(int152).min, type(int152).max);
    }

    function minMaxint160() public pure returns (int160, int160) {
        return (type(int160).min, type(int160).max);
    }

    function minMaxint168() public pure returns (int168, int168) {
        return (type(int168).min, type(int168).max);
    }

    function minMaxint176() public pure returns (int176, int176) {
        return (type(int176).min, type(int176).max);
    }

    function minMaxint184() public pure returns (int184, int184) {
        return (type(int184).min, type(int184).max);
    }

    function minMaxint192() public pure returns (int192, int192) {
        return (type(int192).min, type(int192).max);
    }

    function minMaxint200() public pure returns (int200, int200) {
        return (type(int200).min, type(int200).max);
    }

    function minMaxint208() public pure returns (int208, int208) {
        return (type(int208).min, type(int208).max);
    }

    function minMaxint216() public pure returns (int216, int216) {
        return (type(int216).min, type(int216).max);
    }

    function minMaxint224() public pure returns (int224, int224) {
        return (type(int224).min, type(int224).max);
    }

    function minMaxint232() public pure returns (int232, int232) {
        return (type(int232).min, type(int232).max);
    }

    function minMaxint240() public pure returns (int240, int240) {
        return (type(int240).min, type(int240).max);
    }

    function minMaxint248() public pure returns (int248, int248) {
        return (type(int248).min, type(int248).max);
    }

    function minMaxint256() public pure returns (int256, int256) {
        return (type(int256).min, type(int256).max);
    }
}
